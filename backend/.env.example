# Production Environment Variables Template for qliq.info deployment
# Copy this file to .env.production and fill in your actual values

# Flask Configuration
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars
FLASK_ENV=production
FLASK_DEBUG=False

# Database Configuration
# Note: When using Docker Compose, use 'mysql' as hostname
# The password should match MYSQL_PASSWORD in docker-compose .env file
DATABASE_URL=mysql+pymysql://voiceflow:${MYSQL_PASSWORD}@mysql:3306/voice_flow

# JWT Configuration
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production-min-32-chars

# Frontend URL (for CORS)
# Update this to your actual production frontend domain
FRONTEND_URL=https://qliq.info

# OpenAI API Key (REQUIRED for transcription and analysis)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Google OAuth Configuration (Optional - for Google Sign-In)
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://backend.qliq.info/api/auth/google/callback

# Email Configuration (SMTP)
# For Gmail: Use smtp.gmail.com with App Password (not regular password)
# For Outlook: Use smtp.office365.com
# For other providers: Check their SMTP settings
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# ==============================================================================
# IMPORTANT SECURITY NOTES:
# ==============================================================================
# 1. Never commit .env.production with real secrets to git
# 2. Use strong, unique passwords (minimum 32 characters)
# 3. Store secrets securely (use password manager or secret management service)
# 4. Rotate API keys and passwords regularly
# 5. For Gmail: Create App Password at https://myaccount.google.com/apppasswords
# ==============================================================================
